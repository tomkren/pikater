/**
 * Package containing classes that implements access to ARFF files for visualisation purposes.
 * Each ARFF file is accessed in incremental way, so loading the whole file to memory is not necessary.
 */
package org.pikater.web.visualisation.implementation.datasource;